from enum import Enum


class CircuitMetadataEnum(Enum):
    SUPPLY = ".supply"
    GROUND = ".ground"
    P_TYPE = ".P-type"
    N_TYPE = ".N-Type"
    INPUTS = ".inputs"
    OUTPUTS = ".outputs"
